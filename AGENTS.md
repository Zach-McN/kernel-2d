# AGENTS.md — kernel-2d

The instructions for this repo live in **[CLAUDE.md](CLAUDE.md)**. Read that file; there is
nothing here that is not there.

This is a pointer rather than a copy, on purpose. It arrived as a full duplicate of the
constitution and had drifted from it within minutes — the marking rule said two things in one
file and three in the other. A second copy of a document with authority is a second thing to
keep in step and a coin toss over which one a session obeys, which is the one-source rule this
kernel already holds for its own formats (`editor-kernel` D2 and D6) applied to its own prose.

The same goes for `.agents/skills`: it is a junction to the `gamedev-skills` repo, exactly as
`.claude/skills` is, so both paths reach one library rather than two copies of one.
