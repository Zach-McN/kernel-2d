.claude/settings.local.json

# Junctions/symlinks to the sibling gamedev-skills repo — it lives in its own repo,
# and both of these reach that one copy rather than holding a second.
.claude/skills
.agents/skills

node_modules/
dist/

# Pictures of the editor, taken by `npm run shot`. Made on demand, looked at,
# thrown away — never a baseline anything is compared against.
shots/

# Browser-test output and the throwaway project the browser tests build
test-results/
playwright-report/
tests/.tmp/
