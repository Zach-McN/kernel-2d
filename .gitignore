.claude/settings.local.json

# Junction/symlink to the sibling gamedev-skills repo — lives in its own repo
.claude/skills

node_modules/
dist/

# Browser-test output and the throwaway project the browser tests build
test-results/
playwright-report/
tests/.tmp/
